# Zola build output
/public

# local tooling + Rust build dirs
/.tools
/slm-wasm/target
/slm-wasm/pkg

# /nibli artifacts — fetched & built from github.com/dhilipsiva/nibli at deploy
# time (scripts/build_nibli.sh); never vendored
/static/nibli/wasm
/static/nibli-playground

# /voksa console — fetched & built from github.com/dhilipsiva/voksa at deploy
# time (scripts/build_voksa.sh); never vendored
/static/voksa

# /silicon-eras explorable — fetched & built from github.com/dhilipsiva/silicon-eras
# at deploy time (scripts/build_silicon_eras.sh); never vendored
/static/silicon-eras

# /fanva translator playground — fetched & built from github.com/dhilipsiva/fanva
# at deploy time (scripts/build_fanva.sh); never vendored
/static/fanva

# fine-tune workdirs (env, generated data, checkpoints, >100MB ggufs)
/finetune/.venv
/finetune/data
/finetune/out
/finetune/convert_hf_to_gguf.py
/static/play/models
__pycache__/

# OS / editor cruft
.DS_Store
Thumbs.db
